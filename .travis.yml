sudo: required
dist: trusty
language: node_js
node_js: "8"
install:
    - npm install -g gulp mocha istanbul coveralls
    - npm install
script: "npm run test-travis"
after_script: "cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js"
