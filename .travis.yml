sudo: required
dist: trusty
language: node_js
node_js: "8"
install:
    - npm install -g gulp mocha istanbul istanbul-coveralls coveralls
    - npm install
script:
    - npm run build
after_script: "npm run coveralls"
